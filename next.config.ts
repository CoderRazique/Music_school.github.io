import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:['encrypted-tbn0.gstatic.com', 'upload.wikimedia.org', 'cdns-images.dzcdn.net', 's.saregama.tech', 'images.unsplash.com']
  }
};

export default nextConfig;
